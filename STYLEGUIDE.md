# Лендинг FAROS — свод правил

Один файл `index.html`: разметка, стили и скрипты внутри. Любая правка — внутри этих правил.
Проверка перед публикацией обязательна: `node tools/preflight.mjs` (0 красных пунктов).

## Голос

Говорит поставщик, от первого лица: «поставляем», «подберём», «пришлём», «отгружаем».
Читатель — снабженец промышленного предприятия и служба охраны труда. Он спешит, считает
смету и проверяет документы.

- Короткие утверждения. Одна мысль — одно предложение.
- Заголовок называет содержание блока словами покупателя, а не играет словами.
- Подпись у заголовка говорит то, чего нет в заголовке. Не пересказ.
- Никакого пафоса: «уникальный», «широкий ассортимент», «лучшие решения» — запрещены.
- Никаких вопросов-заглушек в заголовках плашек. Вместо «Не нашли свой случай?» — «Подберём под ваш участок».

## Запреты (нарушение = красный пункт проверки)

- Длинные и средние тире. Только дефис `-`.
- Любые непроверенные утверждения: сроки поставки, наличие на складе, город склада,
  отсрочка платежа, НДС, имена клиентов, гарантии.
- Выдавать нашу собственную разметку за данные производителя (был случай с цветовой
  кодировкой классов — цвета наши, у производителя такой кодировки нет).
- Служебные метки аналитики в тексте, который видит клиент.
- Однобуквенные обозначения, отрывающиеся при переносе: «NR&nbsp;-&nbsp;для», «D&nbsp;-&nbsp;изделие».

## Типографика

| Роль | Шрифт | Размер |
|---|---|---|
| Заголовки, числа, названия моделей | Unbounded, вес 800–900 | по шкале `--t-h2` / `--t-h2-mid` |
| Служебные подписи, кикеры, единицы | IBM Plex Mono, вес 400, капслок | ровно 10 или 11px |
| Обычный текст, цены в таблицах | Manrope | 13–15px |

Заголовочный шрифт обязан содержать и кириллицу, и латиницу: смешанные строки
(«Респираторы FAROS») рисуются одной гарнитурой, без подмены системным шрифтом.
Разрядка моно-подписей - только три ступени: `--ls-ui` / `--ls-key` / `--ls-cap`.

Кикер раздела (`.sec-head .kick`) — всегда моно, вес 400, 11px, фирменный зелёный.
Подпись у заголовка (`.sec-side .note`) — моно капслок 11px во всех разделах без исключения.

## Цвет

- `--brand` #326450 — фирменный зелёный на светлом.
- `--brand-lift` #57AA82 — тот же зелёный на тёмном (контраст).
- `--stage` #0A0C0B — тёмные острова: первый экран, прайс, блок о поставщике, подвал.
- Тёмные блоки не идут подряд: между ними всегда светлый раздел.

## Отступы и сетка

- Вертикальный ритм секций: `clamp(40px, 6vw, 96px)`.
- Осознанные исключения: модельный ряд и прайс сближены (одна мысль), перед заявкой — увеличенный вдох.
- Скругления: 6 / 8 / 12px (`--r-s` / `--r-m` / `--r-l`).
- Цели под палец на телефоне — не меньше 44px по высоте.

## Цены

Единственный источник правды — таблица прайса в разделе «Открытый прайс».
Любая цена в другом месте страницы должна совпадать с ней до тенге.
Прайс-PDF собирается из этой же таблицы: `node tools/price-pdf.mjs` (падает, если позиций не 22).

## Порядок работы

1. Правки по дизайну делает агент-дизайнер, тексты — агент-копирайтер.
2. Перед публикацией: `node tools/preflight.mjs` — ноль красных пунктов.
3. Скриншоты до/после на 390, 820 и 1440 — и посмотреть их глазами.
4. Публикация только после решения владельца.
